import { Container } from "./style.js";
import Filter from "../Filter";

function Header() {

    return (
        <div>
            <Container>
                <Filter />
            </Container>
        </div>
    );
}

export default Header;
